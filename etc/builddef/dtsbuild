VERSION=$(date +%y.%m)
VERSEP="-";
PREFIX="/opt/${BUILDPROJECT=}";
STDOPTS="--prefix=/opt";
ALIAS="buildscripts";
GITBRANCH="master";
BUILDDIR="0";
RPMGROUP="dev";
if [ "${NARCH}" == "${barch}" ];then
  NOINST="1";
fi;
