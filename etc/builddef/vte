VERSION="0.28.2"
VERSEP="-";
PREFIX="/usr";
CONFOPT="";
if [ "${HASGIR}" == "no" ];then
  CONFOPT="${CONFOPT} --disable-python --disable-glade";
fi;
RPMGROUP="gui";
case ${barch} in
  arm*|i686|x86_32)CHROOTBUILD="1";;
esac
