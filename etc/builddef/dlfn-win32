VERSION="svn"
VERSEP="-";
PREFIX="/usr";
CONFOPT="";
GITBRANCH="master";
STDOPTS="--prefix=/mingw --cross-prefix=${HOST}- --enable-shared";
NOCCOPT="1";
if [ "${barch:0:3}" != "win" ];then
  DONTBUILD="${barch}";
fi;
BUILDDIR="0";
RPMGROUP="dev";
