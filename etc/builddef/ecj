VERSION="4.3.2"
VERSEP="-";
PREFIX="/usr";
CONFOPT="";
BUILDDIR="0";
MAKEOPTS="LIBDIR=${B_LIBDIRS} GCJ=${HOST}-gcj";
COREBUILD="1";
RPMOBSOLETE="javac";
RPMGROUP="dev";
JAVA_HOME="/usr/${NLIBDIR}/jvm/jdk-${JAVA7_VER}";
if [ -e /proc/sys/fs/binfmt_misc/${SYSARCH} ];then
  DONTBUILD="${barch}";
fi;
