VERSION="52.1"
VERSEP="-";
PREFIX="/usr";
if [ "${barch}" == "${NARCH}" ];then
  CONFOPT="";
 else
  CONFOPT="--with-cross-build=${SRCDIR}/icu/icu-52.1/build-${NARCH}";
fi;
CFGBIN="source/";
NODIET="1";
