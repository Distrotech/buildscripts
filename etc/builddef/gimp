VERSION="2.8.8"
VERSEP="-";
PREFIX="/usr";
CONFOPT="PYTHON=${ARCHROOT}/${barch}/usr/bin/python --disable-gtktest";
ADDCFLAG="-I=/usr/include/python2.7";
case ${barch} in
  arm|arm64|x86_32)CHROOTBUILD="1";;
esac;
