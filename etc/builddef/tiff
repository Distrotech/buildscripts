VERSION="4.0.3"
VERSEP="-";
if [ "${barch:0:3}" == "win" ];then
  PREFIX="/mingw";
 else
  PREFIX="/usr";
fi;
CONFOPT="";
STDOPTS="--prefix=${PREFIX} --sysconfdir=/etc --mandir=${PREFIX}/share/man --localstatedir=/var --libdir=${PREFIX}/${C_LIBDIRS} --host=${HOST}";
FIXUPLIB="1";
GITBRANCH="master";
MLIBDIRLN="libtiff.so";
