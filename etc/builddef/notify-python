VERSION="0.1.1"
VERSEP="-";
PREFIX="/usr";
CONFOPT="";
CONFOPT="PYTHON=${ARCHROOT}/${barch}/usr/bin/python";
ADDCFLAG="-I=/usr/include/python2.7";
GITBRANCH="master";
if [ "${HASGIR}" == "no" ];then
  DONTBUILD="${barch}";
fi;
case ${barch} in
  powerpc*)CHROOTBUILD="1";;
esac;
