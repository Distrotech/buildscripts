VERSION="0.20.1"
VERSEP="-";
PREFIX="/usr";
CONFOPT="";
CHROOTBUILD="1";
ALIAS="vala";
if [ "${BUILDPROJECT}" == "vala-bootstrap" ];then
  NOINST="1";
fi;
if [ "${HASGIR}" == "no" ];then
  DONTBUILD="${barch}";
fi;
RPMGROUP="obsolete";
