VERSION="2.2.4"
VERSEP="-";
PREFIX="/usr";
CONFOPT="--with-x";
BUILDDIR="0";
ADDLDFLAG="-L../src/elfobjs";
ADDCFLAG="-I../../src/";
RPMPROVIDES_libs="libslang.so.2()${ARCH_BITS} libslang.so.2(SLANG2)${ARCH_BITS} libslang.so.2(SLANG2.1.0)${ARCH_BITS}";
