VERSION="0.8.0"
VERSEP="-";
PREFIX="/usr";
if [ -e /proc/sys/fs/binfmt_misc/${SYSARCH} ];then
  CONFOPT="--disable-bdjava";
 else
  CONFOPT="";
fi;
ADDCFLAG="-I=/usr/include/libxml2 -Ilibbluray";
RPMGROUP="core";
