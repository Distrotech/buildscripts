VERSION="3.10.2"
VERSEP="-";
PREFIX="/usr";
CONFOPT="";
if [ "${HASGIR}" == "no" ];then
  DONTBUILD="${barch}";
fi;
case ${barch} in
  arm*|i686|x86_32|ppc*|mips64)CHROOTBUILD="1";;
esac;
RPMGROUP="gui";
